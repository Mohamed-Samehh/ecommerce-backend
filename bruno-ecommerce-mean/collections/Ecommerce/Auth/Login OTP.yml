info:
  name: Login OTP
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/auth/login/verify-otp"
  body:
    type: json
    data: |-
      {

        "email": "mohamed.kholy2011@gmail.com",
        "otp": "DD726D"

      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
