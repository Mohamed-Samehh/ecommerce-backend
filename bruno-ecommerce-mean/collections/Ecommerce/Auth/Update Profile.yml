info:
  name: Update Profile
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseUrl}}/auth/me"
  body:
    type: json
    data: |-
      {
        "dob": "2002-02-02",
        "password": "123456"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
