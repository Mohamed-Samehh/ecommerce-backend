info:
  name: Update Profile
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/auth/me"
  body:
    type: json
    data: |-
      {
        "dob": "2000-02-02",
        "password": "123456"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
