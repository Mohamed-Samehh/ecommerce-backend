info:
  name: Verify OTP
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/auth/register/verify-otp"
  body:
    type: json
    data: |-
      {
        "email": "mohamed.kholy2011@gmail.com",
        "otp": "45C7C9"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
