info:
  name: updateAuthor
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/authors/:id"
  params:
    - name: id
      value: 699a47c0ceb920e8fd802c5e
      type: path
  body:
    type: json
    data: |-
      {
        "name":"Andrew Emad"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
