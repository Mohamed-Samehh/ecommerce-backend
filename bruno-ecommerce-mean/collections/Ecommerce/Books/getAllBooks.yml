info:
  name: getAllBooks
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/books"
  params:
    - name: limit
      value: "2"
      type: query
      disabled: true
    - name: page
      value: "1"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
