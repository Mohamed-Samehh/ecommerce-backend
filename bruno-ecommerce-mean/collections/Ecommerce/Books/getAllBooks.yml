info:
  name: getAllBooks
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/books?limit=50&page=1"
  params:
    - name: limit
      value: "50"
      type: query
    - name: page
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
