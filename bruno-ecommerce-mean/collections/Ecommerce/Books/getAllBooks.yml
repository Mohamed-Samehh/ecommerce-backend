info:
  name: getAllBooks
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/books?limit=2&page=1"
  params:
    - name: limit
      value: "2"
      type: query
    - name: page
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
