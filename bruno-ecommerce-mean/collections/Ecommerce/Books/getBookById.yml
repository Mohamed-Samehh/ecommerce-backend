info:
  name: getBookById
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/books/:id"
  params:
    - name: id
      value: 699dd7c882aff9c8dca87543
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
