info:
  name: getBookById
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/books/:id"
  params:
    - name: id
      value: 699c2fa969ff88ac0840fb4b
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
