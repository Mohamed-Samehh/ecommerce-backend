info:
  name: UpdateBookQuantity
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/cart/64f8a2b3c9e77b001f3d8a12"
  body:
    type: json
    data: |-
      {
        "quantity": 29
      }
  auth:
    type: bearer
    token: "{{motoken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
