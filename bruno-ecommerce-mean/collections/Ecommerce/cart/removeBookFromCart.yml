info:
  name: removeBookFromCart
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/:bookId"
  params:
    - name: bookId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
