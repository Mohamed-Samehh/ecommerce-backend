info:
  name: createCategories
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/categories"
  body:
    type: json
    data: |-
      {
        "name": "abdelhaq"
      }
  auth:
    type: bearer
    token: "{{motoken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
