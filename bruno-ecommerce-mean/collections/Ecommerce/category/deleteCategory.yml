info:
  name: deleteCategory
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/categories/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
