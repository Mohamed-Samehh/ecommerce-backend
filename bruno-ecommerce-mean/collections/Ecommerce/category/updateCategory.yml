info:
  name: updateCategory
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/categories/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
